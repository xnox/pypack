#!/bin/bash

MY_DIR="$(cd "$(dirname $0)" && pwd)"

PYPACK_ZIP=$MY_DIR/%(zip_file)s

PYTHONPATH=$PYPACK_ZIP
for path_addition in $PYTHONPATH_ADDITIONS
do
    PYTHONPATH=$PYTHONPATH:$PYPACK_ZIP/$path_addition
done

export PYTHONPATH=$PYTHONPATH

/usr/bin/env python -m %(entry_module)s $*

